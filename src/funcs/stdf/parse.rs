use crate::types::{Type, TypeName, Vars};
use crate::{memory::Memory, Registers};

pub fn parse(
    memory: &mut Memory,
    vars: &mut Vars,
    registers: &mut Registers,
    cmd: Vec<&str>,
    b: &str,
    l: i32,
) {

}